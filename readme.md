this repository contains simple basic python programs

1. if you create new file, then you should run the below command to push the code into your github repository

git add .

2. if you want to pusht the file, then before you should commit the files, for this you need to run the below command
git commit -m "give message what you did"

3. if you want to send the code into the github repository, then you should run the below command after above steps completed

git push
